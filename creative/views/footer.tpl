<div class="footer-container">
	<div class="structure footer">
		
		<div class="contact-footer">
			<h3>{{contact_page.first().title}}</h3>
			<span class="address">{{contact_page.first().address}}</span><br />
			<span class="email">{{contact_page.first().email}}</span><br />
			<span class="phone">{{contact_page.first().phone}}</span>
		</div>
	</div>
</div>

<p class="copyright">{{clippings.copyright}}. {{clippings.site_name}}</p>